# Vercel for hosting, GitHub Actions for CI and code scanning

GameTrakr is a Next.js app, and Vercel is built by the Next.js team as the zero-config deployment target for it — preview environments per PR, plus built-in analytics, speed insights, and log access without extra tooling. GitHub Actions runs CI and code scanning since the repo already lives on GitHub.
