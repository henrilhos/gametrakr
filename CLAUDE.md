@AGENTS.md

## Agent skills

### Issue tracker

Issues and specs live as GitHub Issues in `henrilhos/gametrakr`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` at the repo root for the domain glossary, `docs/adr/` for architecture decisions. See `docs/agents/domain.md`.
